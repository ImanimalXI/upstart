var should = require('should');


describe('NAME', function() {

    it('should exist', function(){
        var cart = new NAME();
        cart.should.be.an.instanceOf(Object);
        cart.should.be.an.instanceof(NAME);
    });

});